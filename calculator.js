function calculator (a, b) {
    let add = a + b;
    let subtract = a - b;
    let divide = a / b;
    let multiply = a * b;


    return add, subtract, divide, multiply;
}

module.exports = calculator;